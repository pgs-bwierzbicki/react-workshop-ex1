import React from 'react';
import Navbar from './navbar';
import Title from './title';

const Header = () => {
    return (
        <header>
            <Navbar />
            <Title />
        </header>
    );
};

export default Header;